spring:
  main:
    banner-mode: off

ecm:
  api:
    base-url: http://localhost:8081/api/v1
    username: test_user
    password: test_password
    timeout:
      connect: 5s
      read: 10s
    max-retries: 1

logging:
  level:
    root: WARN
    com.jpmc.ecm: DEBUG
